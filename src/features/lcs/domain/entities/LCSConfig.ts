export interface LCSConfig {
  /**
   * Primeira string da comparação (X).
   * Deve estar normalizada (ex.: maiúsculas, sem caracteres inválidos).
   */
  stringX: string;
  /**
   * Segunda string da comparação (Y).
   * Deve estar normalizada (ex.: maiúsculas, sem caracteres inválidos).
   */
  stringY: string;
}


